# 销售与集客线路系统人员权限配置

> 文档性质：人员权限配置基线，供后续配置、开发、测试和验收使用。  
> 人员数据来源：`C:\Users\jinwa\Desktop\酬金系统\系统权限配置.xlsx`。  
> 功能权限来源：当前项目代码，只表示截至 2026-08-26 的实际实现。  
> 注意：附件 Excel 只提供人员名单和角色归属，没有定义菜单、数据范围、增删改查或审批规则；本文将附件原始配置与代码现状分开记录。

本文件包含姓名和网格通账号，仅限内部权限配置使用。

## 1. 权限模型与维护口径

系统权限按以下顺序生效：

```text
账号状态
  → 工作台（销售 / 集客线路）
    → 基础角色 users.role
      → 集客附加角色 lineProjectRoles
        → 管理区县 managedDistricts
          → 具体业务状态（反馈、签字、导入批次等）
```

维护原则：

1. 本附件配置的是四个管理层级：基层监督员、区县主管、区县经理、系统管理员。
2. 普通销售人员、普通集客装维人员是系统基础用户，不计入附件的四个管理层级。
3. 销售系统区县主管和集客线路系统区县主管必须作为两套配置分别维护；即使当前名单相同，也不得合并成单一字段。
4. 网格通账号是人员匹配、数据归属和审批路由的关键标识，应保持全局唯一。
5. 人员变更时，应同时检查 `users`、`feedback_routes`、代码名单和工作台类型，不能只修改本文件。
6. 本文中的“当前代码权限”是现状记录；带有“待确认”或“建议”的内容不代表已经在系统中生效。

## 2. 四层级与系统字段映射

下表是依据当前代码字段含义整理的后续配置映射。

| 人员层级 | 人员名单来源 | 销售系统配置映射 | 集客线路系统配置映射 | 核心职责 |
| --- | --- | --- | --- | --- |
| 基层监督员 | Excel A:C | `feedback_routes.supervisor` | `feedback_routes.supervisor`、`lineProjectRoles = district_supervisor` | 处理分配给本人的反馈；具体数据查看范围见第 4 节 |
| 区县主管 | 销售：Excel E:G；集客：Excel I:K | `users.role = district_manager` | `feedback_routes.districtLeader`、`lineProjectRoles = district_leader` | 管理本区县业务数据、查看本区县反馈；集客可维护证明材料 |
| 区县经理 | Excel M:O | `feedback_routes.districtManager` | `feedback_routes.districtManager`、`lineProjectRoles = district_manager` | 管理本区县数据并处理分配给本人的反馈 |
| 系统管理员 | Excel Q:R | `users.role = system_admin` | `users.role = system_admin`、`SYSTEM_ADMIN_ROSTER` | 全市数据、导入发布、回滚及权限配置 |

说明：

- 销售代码中的 `district_manager` 中文角色文案是“区县主管”。
- 集客代码中的 `district_manager` 中文角色文案是“区县经理”。
- 集客“区县主管”对应的是附加角色 `district_leader`，不要与 `district_manager` 混用。
- 当前代码还存在 `sales_department` 技术角色，但附件四层名单中没有该层级，是否保留见第 6 节待确认项。

## 3. 附件人员配置

### 3.1 基层监督员名单

| 区县 | 姓名 | 网格通账号 |
| --- | --- | --- |
| 荆州区 | 何治国 | 15972756298 |
| 沙市区 | 成凡江 | 15927933068 |
| 开发区 | 汪昊然 | 15927734831 |
| 监利 | 罗朝刚 | 15007218456 |
| 洪湖 | 罗平 | 15826500088 |
| 松滋 | 梅元江 | 13617271888 |
| 公安 | 张陈新 | 13545676504 |
| 江陵 | 吴道然 | 13697268665 |
| 石首 | 王琼 | 15927972270 |

### 3.2 销售系统区县主管名单

| 区县 | 姓名 | 网格通账号 |
| --- | --- | --- |
| 荆州区 | 王灿 | 18071888614 |
| 沙市区 | 赵青 | 13986655569 |
| 开发区 | 杨芳 | 13545648553 |
| 监利 | 王娟 | 13697247000 |
| 洪湖 | 邹方艳 | 18772699601 |
| 松滋 | 朱锦纯 | 18272229353 |
| 公安 | 李雄 | 18272201885 |
| 江陵 | 黄刚 | 15927919510 |
| 石首 | 鲁浩 | 13508622998 |

### 3.3 集客线路系统区县主管名单

| 区县 | 姓名 | 网格通账号 |
| --- | --- | --- |
| 荆州区 | 王灿 | 18071888614 |
| 沙市区 | 赵青 | 13986655569 |
| 开发区 | 杨芳 | 13545648553 |
| 监利 | 王娟 | 13697247000 |
| 洪湖 | 邹方艳 | 18772699601 |
| 松滋 | 朱锦纯 | 18272229353 |
| 公安 | 李雄 | 18272201885 |
| 江陵 | 黄刚 | 15927919510 |
| 石首 | 鲁浩 | 13508622998 |

销售与集客线路区县主管当前为同一批人员，但此处保留两张独立表，后续可分别调整。

### 3.4 区县经理名单

| 区县 | 姓名 | 网格通账号 |
| --- | --- | --- |
| 荆州区 | 徐家军 | 13797309966 |
| 沙市区 | 郑东 | 13886637788 |
| 开发区 | 王荣进 | 13797555099 |
| 监利 | 白桦 | 15872159705 |
| 洪湖 | 邓敏 | 13872212008 |
| 松滋 | 郑拥华 | 13593882355 |
| 公安 | 徐小红 | 13707212333 |
| 江陵 | 唐山 | 13797336655 |
| 石首 | 刘建康 | 15272602322 |

### 3.5 系统管理员名单

| 姓名 | 网格通账号 |
| --- | --- |
| 刘炽 | 13995951983 |
| 李奇琦 | 15171129197 |
| 陈风 | 13871457088 |
| 张斌 | 18707123688 |
| 王谨 | 15871165073 |

### 3.6 配置统计

- 区县：9 个。
- 基层监督员：9 人。
- 销售系统区县主管：9 条配置。
- 集客线路区县主管：9 条配置。
- 区县经理：9 人。
- 系统管理员：5 人。
- 附件共涉及 32 名不同人员；按系统角色配置记录计算，共 41 条。

### 3.7 附件数据说明

- 基层监督员网格通账号来自 Excel 外部引用公式，外部源文件为“附件4：监督员信息台账2026.5.19.xlsx”。本文保存的是附件当前显示的账号值，不依赖外部工作簿。
- 附件外链缓存中包含的性别、生日等字段与权限配置无关，未写入本文。
- 附件中系统管理员区域没有“姓名、网格通账号”二级表头，本文已按实际两列数据整理。

## 4. 当前代码中的实际功能权限

本节不是附件原始内容，而是为了后续配置两个系统，记录当前代码已经实现的权限边界。

### 4.1 销售系统

| 人员层级 | 销售业务数据范围 | 录入、导入和导出 | 更新、删除 | 反馈权限 |
| --- | --- | --- | --- | --- |
| 普通销售人员（基础用户） | 本人 openid 或网格通账号关联记录 | 可手工录入；不可批量导入；可导出本人数据 | 云端允许更新、删除本人记录 | 可提交并查看本人反馈 |
| 基层监督员 | 不因监督员身份自动扩大业务数据范围，仍继承本人基础角色 | 继承基础角色 | 继承基础角色 | 可查看并审批创建时分配给本人的销售反馈 |
| 区县主管 | 当 `users.role=district_manager` 时为本区县数据 | 可手工录入；可批量导入本区县人员；可导出本区县数据 | 页面不显示删除按钮；云端当前可按 ID 跨区县更新、删除，属于待修复问题 | 不自动获得反馈处理权；只有被路由分配时才能处理 |
| 区县经理 | 当前没有独立的销售业务数据角色，数据范围取决于其 `users.role` | 继承基础角色 | 继承基础角色 | 可查看并审批创建时分配给本人的销售反馈 |
| 系统管理员 | 全部销售数据 | 可手工录入、批量导入和全量导出 | 可更新、删除全部记录 | 当前没有“查看全部销售反馈”权限；仅能处理分配给本人的反馈 |

销售系统当前共同约束和注意点：

- 手工录入接口未限制目标人员范围，各角色当前都可能为任意已存在的网格通账号代录。
- 区县主管批量导入时，目标人员必须属于本区县。
- 销售记录更新接口没有字段白名单，归属、状态和酬金等保护字段存在被修改的风险。
- 销售云函数尚未完整校验 `workspaceType`、停用状态和资料完整状态。
- 当前销售反馈不是双审批：区县经理或基层监督员任意一人先通过或驳回后，另一人会变为 `not_required`，反馈立即结束。

额外技术角色：

- `sales_department` 当前可以查看、导入、导出、更新和删除全部销售数据。
- `sales_department` 不在附件四层名单中，也不会自动获得全市销售反馈查看权。

### 4.2 集客线路系统

| 人员层级 | 集客数据范围 | 导入、发布和导出 | 反馈权限 | 证明材料 | 月度签字 |
| --- | --- | --- | --- | --- | --- |
| 普通集客装维人员（基础用户） | 本人数据、本人概览和本人工单 | 不可导入；云端支持本人范围导出，但当前无页面入口 | 可提交、查看本人反馈 | 不可查看或上传 | 可确认本人当月数据 |
| 基层监督员 | 当前动态角色会获得所管理区县的数据看板和人员、工单明细 | 不可导入；可导出管理区县数据 | 可查看本区县反馈，并答复创建时分配给本人的反馈 | 不可查看或上传 | 云端允许确认本人数据，管理首页当前不显示个人签字区 |
| 区县主管 | 所管理区县的数据看板和人员、工单明细 | 不可导入；可导出管理区县数据 | 可查看本区县反馈，但不能以区县主管身份答复 | 可查看、上传本人管理区县证明材料 | 云端允许确认本人数据，管理首页当前不显示个人签字区 |
| 区县经理 | 所管理区县的数据看板和人员、工单明细 | 不可导入；可导出管理区县数据 | 可查看本区县反馈，并答复创建时分配给本人的反馈 | 可查看、上传本人管理区县证明材料 | 云端允许确认本人数据，管理首页当前不显示个人签字区 |
| 系统管理员 | 全市集客数据 | 可导入预览、分块写入、发布、查看批次、回滚和全量导出 | 可查看全市反馈，但不自动获得答复权 | 可查看全市证明材料，当前不可上传 | 云端允许确认本人数据，管理首页当前不显示个人签字区 |

集客线路系统当前共同约束和注意点：

- 所有普通角色必须属于 `line_project` 工作台；系统管理员云端可例外进入。
- 区县主管、区县经理和基层监督员的最终权限以 `getAccessProfile` 返回的 `lineProjectRoles`、`managedDistricts` 为准。
- 当前只有 `sales_department` 和 `system_admin` 可以导入、发布和回滚；区县主管、区县经理均不能导入。
- 集客没有单条修改或删除明细的接口，数据通过版本化导入、发布和回滚维护。
- 集客反馈要求当月存在本人已发布数据，完成当前版本签字后不能再提交。
- 区县经理或基层监督员任意一人提交答复后，问题即变为 `resolved`，另一处理人变为 `not_required`。
- 有待处理反馈时不能签字；反馈处理结束后可以签字。
- 默认允许同月多次反馈；只有启用 `LINE_PROJECT_MONTHLY_FEEDBACK_LIMIT_ENABLED=true` 后才限制每月一次。

额外技术角色：

- `sales_department` 当前拥有全市集客数据、导入、发布、回滚、导出和全市反馈查看权限。
- `sales_department` 不在附件四层名单中；是否并入系统管理员层级需要业务确认。

## 5. 附件名单与当前代码配置差异

以下差异在正式同步权限前必须处理。

| 序号 | 配置项 | 附件配置 | 当前代码配置 | 处理建议 |
| --- | --- | --- | --- | --- |
| 1 | 基层监督员 | 9 个区县均配置了独立基层监督员 | `APPROVAL_ROUTE_ROSTER.supervisor` 当前仍使用区县主管名单，代码注释为“区县主管暂代基层监督员” | 确认启用附件名单后，将 9 名基层监督员写入审批路由，并停止由区县主管代办 |
| 2 | 监利区县经理白桦账号 | `15872159705` | `APPROVAL_ROUTE_ROSTER` 中为 `15272347267` | 必须人工核对正确账号；在确认前不得直接覆盖运行配置 |
| 3 | 系统管理员 | 5 人：刘炽、李奇琦、陈风、张斌、王谨 | `SYSTEM_ADMIN_ROSTER` 只有刘炽、李奇琦、张斌 3 人 | 确认后补充陈风、王谨，并同步 `users.role` |
| 4 | 销售与集客区县主管 | 附件分别维护两套名单，当前内容相同 | 代码中集客使用 `DISTRICT_LEADER_ROSTER`；销售主要依赖 `users.role=district_manager` | 后续仍按两个系统分别维护和验证，避免一处调整影响另一系统 |
| 5 | 销售业务部角色 | 附件没有该层级 | 代码存在 `sales_department`，且拥有两个系统的大量管理员权限 | 明确保留、降权或合并到系统管理员，不能长期作为未登记的隐含管理员 |

说明：代码中的静态名单不等同于云数据库运行数据。正式变更前，还需要读取并核对云数据库 `users` 和 `feedback_routes` 的实际内容。

## 6. 待业务确认的权限规则

在将本文转为正式配置前，需要明确以下问题：

1. 销售反馈是否继续执行“任一处理人处理即结束”，还是改为区县经理和基层监督员双审批。
2. 集客反馈是否继续执行“任一处理人答复即结束”。
3. 基层监督员是否应查看完整的本区县集客数据看板；当前代码会授予该权限。
4. 区县主管、区县经理是否需要集客导入权限；当前均没有。
5. 区县主管是否只负责查看反馈，还是也应具有反馈答复权；当前集客区县主管只能查看。
6. 系统管理员是否需要上传证明材料；当前只能全市查看，不能上传。
7. `sales_department` 是否继续存在，若保留，其人员名单和职责边界如何配置。
8. 管理人员本人有集客工费时，是否需要在管理首页显示个人签字入口；当前云端允许、页面隐藏。
9. 同一基层监督员和区县经理是否同时处理销售与集客反馈。普通账号当前只有一个主要工作台，可能导致跨系统处理入口冲突。
10. 销售系统区县主管和集客线路区县主管未来是否允许分别调整；本文已按两套名单预留。

## 7. 后续配置检查清单

### 7.1 用户账号

- [ ] 姓名与附件一致。
- [ ] 网格通账号与附件一致且全局唯一。
- [ ] 用户状态为 `active`。
- [ ] 姓名、账号、区县、网格四项资料完整。
- [ ] 销售区县主管正确配置 `users.role=district_manager`。
- [ ] 系统管理员正确配置 `users.role=system_admin`。
- [ ] 工作台类型与实际职责一致。

### 7.2 每个区县的反馈路由

- [ ] `supervisor` 配置为附件基层监督员。
- [ ] `districtManager` 配置为附件区县经理。
- [ ] `districtLeader` 配置为附件集客线路区县主管。
- [ ] 同一区县只有一条有效路由。
- [ ] 路由人员账号均能唯一匹配有效系统用户。
- [ ] 提交人与处理人不存在自审批冲突。

### 7.3 系统权限验证

- [ ] 基层监督员只能看到授权区县或分配给本人的内容。
- [ ] 销售区县主管只能查看、导入、导出本区县销售数据。
- [ ] 集客区县主管只能管理授权区县数据。
- [ ] 区县经理只能管理授权区县并处理分配给本人的反馈。
- [ ] 系统管理员可以查看全市数据和执行导入、发布、回滚。
- [ ] 非系统管理员不能执行系统级名单同步或系统管理员授权。
- [ ] 销售与集客工作台不能通过直接调用云函数越权访问。
- [ ] 停用账号不能继续调用任何业务云函数。

## 8. 相关代码位置

- 工作台和角色文案：`utils/workspace.js`
- 用户登录、资料、工作台激活：`cloudfunctions/userManagement/index.js`
- 销售业务权限：`cloudfunctions/businessData/index.js`
- 集客静态人员名单：`cloudfunctions/lineProjectData/config.js`
- 集客数据权限和名单同步：`cloudfunctions/lineProjectData/index.js`
- 销售与集客反馈权限：`cloudfunctions/salaryFeedback/index.js`

## 9. 变更记录

| 日期 | 变更内容 | 数据来源 | 状态 |
| --- | --- | --- | --- |
| 2026-08-26 | 初次整理四层人员名单、两个系统当前权限及配置差异 | 系统权限配置.xlsx、当前项目代码 | 待业务确认后配置 |
