# 集客线路薪酬可视化系统需求文档

> 文档版本：V1.2（集客线路五模块统一导入版）  
> 编制日期：2026-08-13  
> 需求基线：现有微信小程序、现有云函数及《集客线路导入模板.xlsx》

## 1. 文档目的

本文档用于明确集客线路模块的建设目标、角色权限、业务流程、功能需求、数据规则、验收标准及现有系统改造范围，作为产品评审、原型设计、开发、测试和上线验收的共同依据。

本期统一建设“集客开通、集客维护、集客计次、杆路维护、抢修配置”五个模块，并完整解决以下事项：

1. 一线集客施工员能够按月查看本人业务量、参与项目和薪酬构成。
2. 区县管理人员能够分模块或合并查看本区县全部项目、参与人员、业务量和导入薪酬金额。
3. 系统管理员能够通过一份五工作表模板完成数据预解析、发布、全量查看、反馈监管和数据追溯。

### 1.1 本文档采用的范围边界

更新后的模板已经记录了区县在线下完成分配后，每名施工员在每个工单上的工作量和薪酬金额。该金额就是系统需要导入和展示的薪酬结果，不再区分“原始金额、调整金额、二次分配金额、最终金额”。

本期系统负责五模块数据的统一导入、结构校验、发布、分级展示、问题反馈、月度确认和操作留痕，不提供线上二次分配、分配方案展示、调增调减或分配审批功能。集客开通按工作量和单价重算金额；其余四个模块的模板未提供单价明细，系统以 F 列“支出”为区县线下分配后的权威金额，不自行推算或改写。

## 2. 建设背景与目标

本需求版本实现以下五个子模块：

| 一级分类 | 子模块 | 本文档标准名称 | 备注 |
|---|---|---|---|
| 集客 | 集客开通 | 集客开通 | 工作量明细型模板 |
| 集客 | 集客维护 | 集客维护 | 标准业务量型模板 |
| 集客 | 集客计次 | 集客计次 | 标准业务量型模板 |
| 线路 | 杆路维护 | 杆路维护 | 标准业务量型模板，以工作表名为准 |
| 线路 | 抢修配置 | 抢修配置 | 标准业务量型模板 |

系统应形成“数据导入—人员匹配—规则核验—结果发布—分级查看—异议反馈—处理闭环—月度确认”的完整链路。

核心建设目标如下：

- 薪酬透明：个人可看到区县线下分配并导入的金额、对应工单、工作量项目、数量、单价和计算过程。
- 项目透明：区县管理人员可看到本区县五模块项目由谁参与、各自完成多少工作量或业务量、导入薪酬金额是多少。
- 权限隔离：个人仅看本人；区县人员仅看本区县；系统管理员看全部。
- 数据可信：导入数据必须经过模板、账号、金额和重复数据校验后才能发布。
- 过程可追溯：保留源文件、导入批次、发布版本、处理记录和操作人。
- 问题可闭环：个人可针对具体模块和工单反馈，区县基层监督员和区域经理可审批、处理和办结。

## 3. 术语与口径

| 术语 | 定义 |
|---|---|
| 结算月份 | 薪酬数据所属月份，格式为 `YYYY-MM`，由导入界面选择并写入批次和明细 |
| 导入薪酬金额 | 区县已经完成线下分配后写入模板 F 列的个人工单金额；系统展示以此为准 |
| 系统重算金额 | 仅集客开通根据工作量数量和模板高精度单价重算得到的核对金额 |
| 工单 | 由子模块、项目主题和 ESOP 工单号共同标识的项目 |
| 工作量明细 | 集客开通工作表 G—AP 列的异构计量项目，保留名称、单位、数量和单价 |
| 标准业务量 | 其余四个工作表 E 列的业务量（端数），可按人员、工单和模块汇总 |
| 发布版本 | 经校验后对员工可见的一批五模块薪酬数据版本 |
| 网格通账号 | 人员身份匹配的唯一业务账号；导入时精确匹配 `users.gridAccount` |
| 区域经理 | 用户需求中的区县管理审批人，对应现有反馈路由中的 `districtManager` |
| 基层监督员 | 每个区县配置的监督审批人，对应现有反馈路由中的 `supervisor` |

## 4. 角色与数据权限

### 4.1 角色定义

1. 一线集客施工员：查看本人已发布数据、提交反馈、查看处理进度、完成月度确认。
2. 区县基层监督员：查看本区县项目与导入薪酬结果，受理和审批本区县反馈。
3. 区域经理：查看本区县项目与导入薪酬结果，处理和审批本区县反馈。
4. 系统管理员：导入和发布数据，查看全市全部区县的五模块项目、人员、金额及异常，维护人员和审批路由。

基层监督员和区域经理属于“区县范围权限”，不应仅作为反馈记录上的临时身份。系统应支持一个用户同时具有“本人数据查看”和“区县管理/审批”能力。

### 4.2 权限矩阵

| 功能 | 一线施工员 | 基层监督员 | 区域经理 | 系统管理员 |
|---|---:|---:|---:|---:|
| 查看本人薪酬和工单 | 是 | 是 | 是 | 是 |
| 查看本区县全部项目和人员导入薪酬 | 否 | 是 | 是 | 是 |
| 查看全部区县数据 | 否 | 否 | 否 | 是 |
| 导入、覆盖、回滚、发布数据 | 否 | 否 | 否 | 是 |
| 查看导入异常和未匹配账号 | 否 | 否 | 否 | 是 |
| 提交本人问题反馈 | 是 | 是 | 是 | 是 |
| 处理本区县反馈 | 否 | 是 | 是 | 是（监管/代办） |
| 配置区县审批路由 | 否 | 否 | 否 | 是 |
| 导出本人数据 | 可选 | 可选 | 是，本区县 | 是，全部 |
| 完成月度签字确认 | 是 | 是 | 是 | 按本人数据执行 |

### 4.3 权限强制规则

- 权限必须由云函数校验，前端隐藏按钮不能代替服务端鉴权。
- 一线施工员的数据范围以登录用户不可变的 `openid/userId` 为主，网格通账号为导入匹配键和业务展示键。
- 区县范围从管理员配置的权限关系读取，用户不得通过修改个人资料自行切换管理区县。
- 系统管理员必须通过受控 RBAC 配置授予，禁止通过姓名和网格通账号硬编码自动晋升。
- 已停用用户不得登录业务工作台、导入、查询或审批。

## 5. 总体业务流程

### 5.1 月度数据主流程

1. 系统管理员选择结算月份，上传一份包含五个规定工作表的标准 Excel 文件。
2. 系统按工作表名称依次读取集客开通、集客维护、集客计次、杆路维护和抢修配置。
3. 系统进行预解析：模板识别、字段校验、网格通账号匹配、工单解析、集客开通金额重算及重复数据校验。
4. 页面分别展示可导入数据、阻断错误和金额预警。
5. 阻断错误为零后，管理员确认将该结算月份五模块数据整体替换为当前文件。
6. 系统生成不可变导入批次和待发布数据版本，不直接破坏上一已发布版本。
7. 管理员复核汇总数据后发布。
8. 发布后，一线员工看到本人五模块数据；区县管理人员看到本区县五模块数据；系统管理员看到全部数据。
9. 员工无异议时完成月度确认；有异议时针对具体明细提交反馈。
10. 基层监督员和区域经理处理反馈，处理结论和修改版本全程留痕。

### 5.2 数据重新导入流程

- 重新导入必须生成新版本，不允许先删除已发布数据再逐行写入。
- 每次正式导入是该结算月份五模块的全量替换，文件中完全缺少的区县不保留该月旧数据；系统需展示旧记录数、新记录数和金额影响。
- 新版本全部写入成功并通过汇总校验后，方可原子切换为当前发布版本。
- 失败时继续保留旧版本，不得出现半批数据。
- 若该月份已有员工确认或反馈，重新发布前必须给出强提示，并按业务规则使旧确认失效或要求重新确认。

### 5.3 反馈处理流程

目标状态建议为：

`待受理 → 处理中 → 待双方确认 → 已办结`

异常分支包括：`待补充材料`、`已驳回`、`已撤回`、`重新打开`。

- 员工选择月份、模块、工单和可选工作量项，填写问题类型、争议金额、说明并上传凭证。
- 基层监督员和区域经理均可查看本区县待办。
- 处理人必须填写处理意见；通过、驳回或确认数据有误均要记录操作人、时间和处理结论。
- 若仍沿用双审批，任一人驳回则为驳回，两人均通过才办结；审批应支持意见且避免并发覆盖。
- 审批待办入口必须独立存在，不能受审批人本人是否已确认薪酬影响。

## 6. 功能需求

### 6.1 登录、身份与工作台

| 编号 | 需求 |
|---|---|
| FR-AUTH-01 | 用户微信登录后，系统读取其人员资料、网格通账号、工作台、角色和区县权限。 |
| FR-AUTH-02 | 未绑定集客线路工作台的用户不得访问集客数据。 |
| FR-AUTH-03 | 用户必须完善姓名、网格通账号、区县和所属网格；涉及角色、管理区县和账号唯一性的字段由管理员控制或经过审批。 |
| FR-AUTH-04 | 支持一个用户拥有基础角色和附加权限，例如普通施工员同时是基层监督员。 |
| FR-AUTH-05 | 系统管理员可配置用户状态、角色、工作台、管理区县和审批身份。 |

### 6.2 系统管理员导入中心

| 编号 | 需求 |
|---|---|
| FR-IMP-01 | 仅系统管理员可执行正式导入、覆盖、发布和回滚；其他角色不可因前端参数获得权限。 |
| FR-IMP-02 | 导入时必须选择结算月份；文件必须同时包含五个规定名称的工作表，模块由工作表名确定。 |
| FR-IMP-03 | 支持 `.xls`、`.xlsx` 文件；保留源文件 ID、文件名、校验值、导入人和时间。 |
| FR-IMP-04 | 先预解析后提交。预览显示总行数、人员数、账号数、工单数、区县数、表内金额、系统重算金额、阻断数和预警数。 |
| FR-IMP-05 | 账号不存在、账号重复对应多个用户、同一账号在文件内对应多个姓名、姓名/区县与用户资料冲突时阻断发布。 |
| FR-IMP-06 | 支持导出错误明细，错误至少包含源行号、账号、姓名、工单和原因。 |
| FR-IMP-07 | 集客开通按高精度单价重算，差异超过 0.01 元时产生金额预警；其余四模块因模板无单价明细，不进行推算。 |
| FR-IMP-08 | 同一人同一工单允许多行，必须保留源行并在查询时聚合，不得错误去重。 |
| FR-IMP-09 | 导入记录必须版本化；正式发布以完整版本切换实现，支持查看批次详情和回滚到上一发布版本。 |
| FR-IMP-10 | 页面展示覆盖影响和数据差异，管理员二次确认后方可发布。 |
| FR-IMP-11 | 预览必须按模块展示有效行、错误行、业务量和薪酬合计；任一工作表存在阻断错误时不得发布整批数据。 |

### 6.3 人员匹配与账号管理

| 编号 | 需求 |
|---|---|
| FR-MATCH-01 | 更新模板 C 列“网格通账号”是人员主匹配键，系统使用精确匹配，不再通过姓名推测账号。 |
| FR-MATCH-02 | `users.gridAccount` 在有效用户中必须唯一，并由数据库唯一约束和应用校验共同保证。 |
| FR-MATCH-03 | 匹配成功后，明细固化用户 `openid/userId`；后续查询主要按该不可变标识授权。 |
| FR-MATCH-04 | 姓名和区县作为一致性校验项。与系统资料不一致时不得静默匹配，应进入异常处理。 |
| FR-MATCH-05 | 支持管理员查看未匹配、重复账号、跨区县和已停用账号，并在人员绑定中心处理。 |
| FR-MATCH-06 | 历史记录保留导入时姓名、区县和账号快照，资料变更不得改变历史数据归属。 |

### 6.4 一线员工个人薪酬可视化

| 编号 | 需求 |
|---|---|
| FR-MY-01 | 默认展示当前月份，支持切换月份并自动刷新。 |
| FR-MY-02 | 首页展示本人五模块薪酬总额、各模块金额、参与工单数、明细数、确认状态和反馈状态。 |
| FR-MY-03 | 员工只能查看本人已发布数据，不能看到同工单其他参与人员的个人薪酬。 |
| FR-MY-04 | 工单列表展示项目名称、工单号、本人导入薪酬金额和本人工作量摘要。 |
| FR-MY-05 | 集客开通详情展示工作量名称、单位、数量、单价和计算金额；其余四模块展示业务量、完成日期、公司分类、站点级别、端别和导入薪酬。 |
| FR-MY-06 | 数据为空时明确显示“本月暂无已发布数据”，禁止将空数据误认为 0 元并完成确认。 |
| FR-MY-07 | 员工可从具体工单或工作量项直接发起问题反馈，系统自动携带上下文。 |

### 6.5 区县项目与导入薪酬结果

| 编号 | 需求 |
|---|---|
| FR-DIST-01 | 基层监督员和区域经理可查看其授权区县的全部已发布数据。 |
| FR-DIST-02 | 区县看板支持全部模块或单模块筛选，展示当月总金额、标准业务量、人员数、工单数和金额异常数。 |
| FR-DIST-03 | 支持按月份、工单号、项目名称、人员、网格通账号和确认/反馈状态筛选。 |
| FR-DIST-04 | 工单列表展示导入工单总额、参与人数和工作量摘要。 |
| FR-DIST-05 | 工单详情展示所有参与人员及每人的账号、工作量项、数量、单价、导入薪酬金额和金额占比，并汇总人员金额之和作为工单总额。 |
| FR-DIST-06 | 人员汇总展示每人的五模块金额和参与工单数，可下钻至人员—模块—工单明细。 |
| FR-DIST-07 | 区县管理角色可导出本区县权限范围内的工单汇总和人员明细。 |

### 6.6 系统管理员全局数据中心

| 编号 | 需求 |
|---|---|
| FR-ADMIN-01 | 展示全部区县五模块的总金额、业务量、人员数、工单数和异常数。 |
| FR-ADMIN-02 | 支持“全市 → 区县 → 工单 → 人员 → 工作量项”逐级下钻。 |
| FR-ADMIN-03 | 支持按月份、区县、工单、人员、账号、批次和发布版本查询。 |
| FR-ADMIN-04 | 支持导出当前权限和筛选条件下的区县汇总、工单汇总、人员明细、工作量明细及异常清单。 |
| FR-ADMIN-05 | 提供导入批次、发布版本、回滚、账号匹配、审批路由和操作审计入口。 |

### 6.7 问题反馈与处理中心

| 编号 | 需求 |
|---|---|
| FR-FB-01 | 反馈关联提交人、月份、具体模块和发布版本，并可关联工单、工作量项和导入源行。 |
| FR-FB-02 | 提交字段包括问题类型、问题说明、争议金额和可选附件；说明不得为空。 |
| FR-FB-03 | 员工可查看自己的反馈时间线、当前处理人、双方意见和最终结果。 |
| FR-FB-04 | 基层监督员和区域经理拥有独立待办、处理中和已办列表。 |
| FR-FB-05 | 处理动作至少包括受理、要求补充、通过、驳回、办结；所有动作必须记录意见、人员和时间。 |
| FR-FB-06 | 每个区县必须配置有效的区域经理和基层监督员，二者不得为同一人，提交人不得审批本人反馈。 |
| FR-FB-07 | 若反馈结论确认导入金额有误，应由管理员取得区县修正后的表格并重新导入、发布新版本；禁止在反馈页面直接修改金额或覆盖历史导入数据。 |
| FR-FB-08 | 已签字数据原则上不可新增普通反馈；若管理员重发版本或发现系统性错误，系统应使旧确认失效并开放重新反馈。 |

### 6.8 月度确认

| 编号 | 需求 |
|---|---|
| FR-CFM-01 | 员工仅能对存在已发布数据的月份进行确认。 |
| FR-CFM-02 | 确认记录必须绑定当月五模块发布版本集合、月份和本人总金额快照。 |
| FR-CFM-03 | 存在待处理反馈时不得确认；确认后普通情况下不得再提交该版本反馈。 |
| FR-CFM-04 | 同一用户、月份、场景和发布版本仅允许一条有效确认记录。 |
| FR-CFM-05 | 发布新版本后，旧版本确认标记为失效，员工需对新版本重新确认。 |

## 7. 集客线路五模块模板数据契约

### 7.1 模板概况

导入文件必须包含 `集客开通`、`集客维护`、`集客计次`、`杆路维护`、`抢修配置` 五个工作表。模块归属以工作表名称为准，不以样例行中的工单文本或公司分类字段推断。

`集客开通` 使用范围为 A—AP，第 1—5 行为表头和单价区，第 6 行起为业务数据。固定列如下：

固定列如下：

| 列 | 字段 | 必填 | 规则 |
|---|---|---:|---|
| A | 序号 | 是 | 用于源数据追踪，不作为唯一业务键 |
| B | 区县 | 是 | 取值为公安、洪湖、监利、江陵、荆州区、开发区、沙市区、石首、松滋 |
| C | 网格通账号 | 是 | 精确匹配有效用户；按个人使用时必须唯一 |
| D | 姓名 | 是 | 与账号对应用户姓名一致 |
| E | 工单名称 | 是 | 推荐格式：`子模块#项目主题#ESOP工单号` |
| F | 工单支出（公式） | 是 | 模板保存的个人该行金额，数值型、保留 2 位展示 |
| G—AP | 36 项开通工作量 | 至少一项大于 0 | 各项可为小数，空白按 0 处理 |

其余四个工作表使用统一的 A—K 结构，第 1 行为表头，第 2 行起为数据：

| 列 | 字段 | 必填 | 规则 |
|---|---|---:|---|
| A | 序号 | 是 | 用于源数据追踪 |
| B | 区县 | 是 | 与账号所属区县一致 |
| C | 网格通账号 | 是 | 精确匹配有效用户 |
| D | 姓名 | 是 | 与账号对应姓名一致 |
| E | 业务量（端数） | 是 | 数值型，可按模块、人员和工单汇总 |
| F | 支出 | 是 | 区县线下分配后的权威薪酬金额 |
| G | 工单名称1 | 是 | 项目与工单识别文本 |
| H | 工单完成日期 | 否 | 按模板原文保存和展示 |
| I | 工单名称2（公司分类） | 否 | 作为分类快照保存，不决定工作表模块 |
| J | 集客客户站点级别 | 否 | 站点级别快照 |
| K | A端/Z端 | 否 | 端别快照 |

### 7.2 工作量分组

| 分组 | 列 | 项目数 |
|---|---|---:|
| 杆路 | G—J | 4 |
| 光缆安装 | K—N | 4 |
| 网络箱安装 | O—Q | 3 |
| 设备安装 | R—U | 4 |
| 终端安装 | V—Y | 4 |
| 综合布线 | Z | 1 |
| 其它 | AA—AE | 5 |
| 复杂设备安装 | AF—AH | 3 |
| 赔补费 | AI—AK | 3 |
| 新增取费标准 | AL—AP | 5 |

### 7.3 金额计算规则

集客开通第 5 行 G—AP 存放高精度单价。每条记录金额应按以下规则计算：

`系统重算金额 = ROUND(SUM(各工作量数量 × 对应高精度单价), 2)`

实现要求：

- 保留模板原始单价精度参与运算。
- 不得先把每个单价或每项金额四舍五入到 2 位后再汇总。
- 最终总和四舍五入到 2 位，与 F 列比较。
- 差异绝对值不超过 0.01 元视为一致，否则记为金额预警。
- 员工端、区县端和管理员端展示的薪酬金额以模板 F 列“导入薪酬金额”为准；系统重算金额只用于校验和异常提示，不得自动替换区县已经分配并导入的金额。
- 模板 F 列虽然名为“公式”，当前文件中保存的是静态数值，没有 Excel 公式单元格。
- 集客维护、集客计次、杆路维护、抢修配置工作表没有单价明细，系统不通过业务量反推金额，不产生虚假的“系统重算差异”。

### 7.4 工单与人员规则

- 工单号建议校验为数字字符串，长度作为可配置规则，不以附件样例中的固定长度作为验收条件。
- 工单业务键应至少包含子模块和工单号，项目主题作为展示快照，不能仅用名称哈希代替稳定业务键。
- 一个工单可由多人参与。
- 同一人同一工单可出现多行，系统保留源行并在展示时合并。
- 账号匹配成功后，姓名、区县不一致属于阻断错误，不能静默覆盖用户资料。
- 附件中的样例行仅用于说明页面展示和多人参与同一工单的效果，不作为正式人员匹配结果。正式导入仍按网格通账号精确匹配；若同一账号对应多个姓名，系统必须提示异常并阻断发布。

### 7.5 功能展示示例（不作为校验或验收基准）

附件中的样例数据用于说明“一个工单可由多人参与，区县端可查看每个人的工作量和金额分布”这一功能需求。系统不得在代码中写死样例行数、姓名、账号、工单号、人数或金额，也不要求正式导入文件复现以下结果。

以附件中的工单 `2716202603023707172901` 为界面展示示例：

| 人员 | 金额（元） | 主要工作量 |
|---|---:|---|
| 刘强 | 165.98 | 光缆附挂 0.34、用户光缆测试 0.5、互联网终端 1、资料费 1、电路跳纤 1 |
| 刘雄 | 141.35 | 光缆附挂 0.33、现场预勘 1 |
| 刘昌俊 | 163.57 | 光缆附挂 0.33、用户光缆测试 0.5、综合布线 1、用户设备调试 1 |
| 合计 | 470.90 | 3 人参与 |

说明：以上内容只用于产品原型和展示口径说明，不属于模板结构校验、金额核验或上线验收用例。正式校验应依据字段、账号、工作量、单价和金额规则动态执行。

## 8. 目标数据模型

现有集合可保留并升级，同时建议新增发布版本和反馈处理日志集合。系统不建设线上二次分配相关集合。

| 集合 | 用途 | 关键字段/改造点 |
|---|---|---|
| `users` | 用户主数据 | openid、唯一网格通账号、姓名、工作台、状态；角色与管理范围不得由用户自行修改 |
| `line_project_records` | 导入源明细 | 月份、模块、区县、账号、userOpenid、工单、工作量数组、模板金额、重算金额、源行、批次和版本 |
| `line_project_import_batches` | 导入批次 | 文件校验值、模板版本、统计、错误、预警、覆盖范围、创建人和状态 |
| `line_project_publish_versions` | 发布版本（新增） | 月份、模块、范围、批次、版本号、状态、发布时间、发布人、上一个版本 |
| `user_person_bindings` | 历史/特殊人员绑定 | 正常主流程改为账号直配；仅处理经授权的特殊绑定，并保留审计 |
| `salary_feedbacks` | 反馈主单 | 增加工单、工作量项、版本、问题类型、争议金额、附件、处理状态和结论 |
| `salary_feedback_logs` | 反馈处理日志（新增） | 动作、意见、操作者、前后状态、时间和附件 |
| `feedback_routes` | 区县审批路由 | 每区县唯一有效配置、区域经理、基层监督员、有效期和代理人 |
| `line_project_month_confirms` | 月度确认 | 绑定发布版本和金额快照，增加有效/失效状态和唯一约束 |

## 9. 数据状态与发布规则

### 9.1 导入批次状态

`uploaded → parsed → validation_failed / ready → committed → published → superseded / rolled_back`

### 9.2 数据可见性

- `uploaded/parsed/ready/committed` 数据仅系统管理员可见。
- 个人和区县管理人员只能看到 `published` 版本。
- 新版本发布后，旧版本改为 `superseded`，历史仍可审计但不计入当前金额。
- 回滚通过重新激活上一完整版本实现，不直接删除审计数据。

## 10. 非功能需求

### 10.1 安全与隐私

- 核心集合禁止客户端直接全量读取，业务数据统一通过云函数按行级权限返回。
- 身份证等非必要敏感信息不得进入本期模板和明细库；网格通账号在非管理场景可按需要脱敏。
- 所有查询、导出、导入、发布、回滚、审批和权限变更写入操作审计。
- 服务端白名单校验 `workspaceType`、场景、子模块、状态和操作，不信任客户端传入角色或范围。
- 防止越权、批量遍历、停用用户访问及同一审批人的并发覆盖。

### 10.2 一致性与可靠性

- 数据版本发布必须具备事务等价的原子性：要么整批成功，要么维持旧版本。
- 同一文件和相同业务范围重复导入时提示幂等冲突。
- 金额使用适合财务计算的精确表示，禁止二进制浮点多次提前舍入造成累计误差。
- 人员金额之和、工单金额、区县金额和全市金额必须逐级可对账。

### 10.3 性能与易用性

- 列表采用服务端筛选和分页，不应把全量数据下载到小程序后再筛选。
- 常用月度看板在正常网络下目标 3 秒内完成首屏展示。
- 导入大文件显示进度或任务状态，避免前端长时间无响应和云函数超时。
- 金额统一保留 2 位小数，工作量保留模板实际精度。

## 11. 现有系统能力与差距

| 领域 | 已有能力 | 主要差距/风险 | 优先级 |
|---|---|---|---:|
| 个人可视化 | 已支持五模块总额、模块金额和按模块工单下钻 | 标准业务量型模块以模板现有字段展示；后续字段变更需同步模板版本 | 已实现 |
| 区县/全局查询 | 已有按全部/单模块看板、按人员、按工单、详情和导出页面 | 确认率、反馈处理率等运营指标仍可继续增强 | 已实现/P1 |
| 本期模块范围 | 五模块已统一接入 | 模板中的工作表名称属于固定数据契约 | 已实现 |
| 导入解析 | 已按两类表结构解析五个工作表并分模块汇总 | 大文件异步任务和错误清单文件仍可增强 | 已实现/P1 |
| 人员匹配 | 已按网格通账号精确匹配，并核对姓名和区县 | 需在云数据库维持账号唯一性 | 已实现 |
| 覆盖导入 | 已保留批次、发布状态、旧版本和回滚能力 | 云数据库不支持整批原生长事务，仍需上线压测异常恢复 | 已实现/P1 |
| 角色安全 | 系统管理员使用 `system_admin` 角色，区县范围由监督员/经理路由授予 | 需由运营流程维护管理员和经理角色 | 已实现 |
| 数据隔离 | 核心业务通过云函数按本人、授权区县和全局范围查询 | 部署时必须同步应用数据库安全规则 | 已实现/部署项 |
| 反馈 | 已有区县经理+基层监督员双审批 | 仅自由文本和通过/驳回，不能关联明细，无处理过程和意见 | P1 |
| 月度确认 | 已有确认与反馈互斥 | 允许 0 元确认、不绑定版本、重导后确认不失效 | P1 |
| 区县导入薪酬结果 | 模板已包含区县在线下完成分配后的个人金额 | 系统只需按权限展示导入结果，不建设线上分配方案或金额调整功能 | 本期边界 |
| 导出 | 后端已有工单汇总和人员明细 Excel | 前端没有管理导出入口，字段未覆盖发布版本和导入薪酬金额口径 | P1 |

### 11.1 当前导入阻断问题

本次五模块改造已处理以下历史阻断问题：

1. `config.js` 使用 `sheetName/validationSheetName`，解析器读取 `mainSheetName/validationSheetNames`。
2. 解析器使用的 `CURRENT_MAJOR_CATEGORY/CURRENT_SUBCATEGORY` 未在云端配置中定义和导出。
3. 现有配置按 A—H 固定列、I—AR 工作量解析；更新模板为 A—F 固定列、G—AP 工作量。
4. 代码要求模板存在“业务量”和两列金额，更新模板均不存在。
5. 代码按姓名、区县及旧身份证哈希推测人员，更新需求明确按网格通账号精确匹配。
6. 当前金额计算会提前把高精度单价和项目金额舍入到 2 位，与模板最终汇总后舍入的规则不一致。
7. 旧附件只有单工作表；新模板改为五个模块工作表，解析器按各自结构读取，不再依赖“按人员”和“按线路工单”校验表。

## 12. 实施优先级建议

### 第一阶段：五模块可信导入与个人可视化（P0）

- 固化五工作表模板数据契约，分别实现集客开通工作量型和其他四模块标准业务量型解析。
- 建立网格通账号唯一匹配、异常处理和不可变用户归属。
- 重构管理员身份、角色范围和数据库安全规则。
- 建立导入批次、发布版本、原子切换和回滚。
- 完成个人五模块金额概览、模块入口、工单、工作量或业务量明细。

### 第二阶段：五模块区县端与管理员端（P0/P1）

- 完成区县看板、按工单、按人员和导入薪酬结果穿透页面。
- 完成管理员全局看板、区县对比、异常中心和导出。
- 为基层监督员配置本区县只读和反馈处理权限。

### 第三阶段：五模块反馈闭环与确认（P1）

- 反馈关联具体工单和明细，增加附件、意见、处理日志和独立待办中心。
- 月度确认绑定发布版本，重发后自动失效并重新确认。
- 完善审批并发控制、路由管理和处理时效统计。

## 13. 首期验收标准

1. 管理员可选择月份并成功预解析同时包含五个规定工作表的文件；缺少任一工作表时明确阻断。
2. 模板账号可唯一匹配用户；不存在、重复、同账号多姓名和跨区异常均被准确拦截并可导出。
3. 集客开通能按工作量与高精度单价动态重算；其余四模块正确展示业务量和 F 列支出，不依赖附件样例中的固定数据。
4. 同一工单存在多名参与人时，区县端能动态展示参与人员、每人工作量、导入薪酬金额和工单汇总金额；不得写死示例工单或示例金额。
5. 一线员工能查看本人五模块金额并逐模块下钻，且不能通过修改请求参数查看他人或其他区县数据。
6. 基层监督员和区域经理只能看到授权区县的五模块项目、人员、业务量和导入薪酬数据。
7. 系统管理员可查看全部区县，并按区县、工单和人员下钻及导出。
8. 系统所有薪酬展示均以导入表格中的薪酬结果为准，不出现线上二次分配方案、调整金额或分配审批入口。
9. 重复发布或中途失败不会破坏上一已发布版本，管理员可回滚。
10. 员工可从具体工单发起反馈，两名审批人可独立处理并保留意见和完整日志。
11. 月度确认绑定发布版本；重发数据后旧确认失效，员工能看到新版本并重新确认。
12. 停用用户、前端伪造角色或伪造场景均无法越权访问和操作。
13. 集客开通、集客维护、集客计次、杆路维护、抢修配置均能从同一导入批次发布，并可按全部模块或单模块查询和导出。

## 14. 待业务确认事项

以下问题不会改变“五模块统一导入、不建设线上二次分配”的范围，但应在上线前确认：

1. 正式数据是否确保“一名施工员对应一个唯一网格通账号”？如果存在共享账号，需要另行提供人员唯一标识。
2. 金额预警是否允许管理员强制发布；如允许，是否需要二次授权和说明？
3. 反馈处理的两名审批人是并行审批、顺序审批，还是一人处理一人复核？
4. 数据发布后员工的确认时限、反馈时限和逾期规则是什么？
5. 区县管理人员是否允许导出含完整网格通账号的数据，还是默认脱敏？

如后续为标准业务量型模块补充单价或计价规则，应提升模板版本并新增对应校验规则，不能用当前样例数据推断单价。

## 15. 现有实现追踪索引

为便于开发时从需求回溯到代码，当前主要实现入口如下：

| 领域 | 当前文件 |
|---|---|
| 集客数据云函数、权限、导入、聚合、确认和导出 | `cloudfunctions/lineProjectData/index.js` |
| 五模块模板字段和集客开通工作量配置 | `cloudfunctions/lineProjectData/config.js` |
| 反馈创建、路由和双审批 | `cloudfunctions/salaryFeedback/index.js` |
| 用户登录、资料和角色查询 | `cloudfunctions/userManagement/index.js` |
| 数据集合与索引说明 | `cloudfunctions/database-config.js`、`cloudfunctions/database-init.js` |
| 集客个人首页 | `pages/line-project/index.*` |
| 集客导入页面 | `pages/line-project/import.*` |
| 本人工单页面 | `pages/line-project/workorders.*` |
| 反馈和审批页面 | `pages/feedback/feedback.*` |
| 集客前端模块与工作量配置 | `utils/line-project-config.js` |
| 集客云函数和导出封装 | `utils/line-project-service.js` |
| 工作台和角色文案 | `utils/workspace.js` |

---

本文档结论以五个工作表中的“C 列为网格通账号”为人员匹配数据契约。后续模板结构、字段或计价规则变更时，应提升模板版本并保留旧版本解析能力，不能直接覆盖历史规则。
